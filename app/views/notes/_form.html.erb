<%= form_with model: @note, local: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content %>
  <%= f.rich_text_area :content %>
  <br>
  <%= f.submit %>
<% end %>
