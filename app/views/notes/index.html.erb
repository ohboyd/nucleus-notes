<h1>My Envelope</h1>

<% @notes.each do |note| %>
  <h2><%= link_to note.title, note %><span><%= " (created #{time_ago_in_words(note.created_at)} ago)" %></span></h2>
  <p><%= note.content %></p>
<% end %>

<%= link_to 'New Note', new_note_path %>
